import TextareaAutosize from 'react-textarea-autosize'
import { Frames } from 'components'

// import dynamic from 'next/dynamic'
// const RapidBoard = dynamic(() => import('components/RapidBoard'), {
//   ssr: false,
// })

export default function Home() {
  return <Frames />
}
